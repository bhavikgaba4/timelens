import type { AnalyticsView } from "./dashboard-types";

interface AnalyticsPanelProps { analytics: AnalyticsView; }

export function AnalyticsPanel({ analytics }: AnalyticsPanelProps) {
  const maxDuration = Math.max(1, ...analytics.durationComparison.flatMap((item) => [item.estimatedMinutes, item.actualMinutes]));
  return <section className="rounded-2xl border border-slate-800 bg-slate-900/70 p-5 shadow-xl shadow-slate-950/20"><div className="flex items-center justify-between"><div><h2 className="font-semibold text-white">Learning from completed work</h2><p className="mt-0.5 text-xs text-slate-400">Historical estimates compared with actual time</p></div><span className="text-xl text-emerald-300">↗</span></div><div className="mt-5 grid grid-cols-2 gap-3"><div className="rounded-xl bg-slate-950/50 p-3"><p className="text-[10px] font-medium uppercase tracking-wider text-slate-500">Mean absolute error</p><p className="mt-1 text-xl font-semibold text-white">{analytics.meanAbsoluteError}m</p></div><div className="rounded-xl bg-slate-950/50 p-3"><p className="text-[10px] font-medium uppercase tracking-wider text-slate-500">Completed tasks</p><p className="mt-1 text-xl font-semibold text-white">{analytics.completedTaskCount}</p></div></div><div className="mt-5"><div className="mb-3 flex items-center justify-between"><h3 className="text-xs font-medium text-slate-300">Estimate vs actual</h3><span className="text-[10px] text-slate-500">cyan = estimate · green = actual</span></div><div className="space-y-3">{analytics.durationComparison.slice(0, 4).map((item) => <div key={item.label}><div className="mb-1 flex justify-between gap-3 text-[10px] text-slate-400"><span className="truncate">{item.label}</span><span>{item.estimatedMinutes}m / {item.actualMinutes}m</span></div><div className="space-y-1"><div className="h-1.5 overflow-hidden rounded-full bg-slate-800"><div className="h-full rounded-full bg-cyan-400" style={{ width: `${(item.estimatedMinutes / maxDuration) * 100}%` }} /></div><div className="h-1.5 overflow-hidden rounded-full bg-slate-800"><div className="h-full rounded-full bg-emerald-400" style={{ width: `${(item.actualMinutes / maxDuration) * 100}%` }} /></div></div></div>)}</div></div><div className="mt-5 border-t border-slate-800 pt-4"><h3 className="text-xs font-medium text-slate-300">Category bias</h3><div className="mt-3 flex flex-wrap gap-2">{analytics.categoryBiases.map((bias) => <span key={bias.category} className="rounded-lg border border-slate-700 bg-slate-950/50 px-2 py-1.5 text-[11px] text-slate-300">{bias.category} <b className="ml-1 text-cyan-200">{bias.factor.toFixed(2)}×</b></span>)}</div></div></section>;
}
